{
  "name": "@davekinkead/reloc",
  "version": "0.0.2",
  "description": "A a zero dependency library for converting auto incrementing integers into reversable, deterministic, hard-to-guess, short IDs",
  "keywords": [ "short IDs", "short-ids", "hashes", "crypto", "reversable"],
  "type": "module",
  "main": "index.js",
  "types": "./types.d.ts",
  "scripts": {
    "test": "tsc && mocha **/*.test.js",
    "build": "tsc"
  },
  "author": "Dave Kinkead <dave@kinkead.com.au>",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.4.2",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6"
  },
  "directories": {
    "test": "tests"
  }
}
